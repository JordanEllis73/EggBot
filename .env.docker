# Docker Build Configuration
# Uncomment the line below if you experience BuildKit metadata resolution issues
# DOCKER_BUILDKIT=0
# COMPOSE_DOCKER_CLI_BUILD=0

# BuildKit settings for better ARM builds on Raspberry Pi
BUILDKIT_PROGRESS=plain
BUILDX_NO_DEFAULT_ATTESTATIONS=1